/* =========================================================================
   WaBlast frontend config
   -------------------------------------------------------------------------
   👉 PASTE YOUR DEPLOYED BACKEND URL HERE (from Railway), e.g.
        window.WABLAST_API_BASE = "https://wablast-backend-production.up.railway.app";

   • Leave it as "" for local development (uses http://localhost:8000).
   • Do NOT put a trailing slash — it's stripped automatically anyway.
   • You can also set it later from the app: Settings → Backend API Connection.
   ========================================================================= */
window.WABLAST_API_BASE = "";
